#include <stdio.h>
#include <string.h>

int main () {
	   char string[55];

	      strcpy(string, "\"Programming is like building a multilingual puzzle");
	         puts(string);
		    
		    return(0);
}
